
import React from 'react';
import SidebarLayout from '@/components/SidebarLayout';
import ThemeTestComponent from '@/components/brand/ThemeTestComponent';

const TestesTema = () => {
  return (
    <SidebarLayout>
      <ThemeTestComponent />
    </SidebarLayout>
  );
};

export default TestesTema;
